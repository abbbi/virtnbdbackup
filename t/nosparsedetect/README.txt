This test uses the arch Linux cloud image to create multiple backups with
different actions in between.

This testcase is used to do mixed backups with --no-sparse-detect option
on a virtual machine.
